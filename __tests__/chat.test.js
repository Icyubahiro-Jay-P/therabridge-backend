/**
 * Chat controller tests have been split into focused files:
 *   - chat.messaging.test.js      (DM send/edit/unsend, markConversationRead)
 *   - chat.community.test.js      (community CRUD, membership, moderation)
 *   - chat.communityMessages.test.js (community message edit/unsend, screenshot reporting)
 *
 * Each file is self-contained with its own mocks and helpers.
 * This file is kept for backward compatibility with import paths.
 */
